<!-- AUTO-LATTICE-RT BEGIN -->
<!--
  Everything between the AUTO-LATTICE-RT sentinels is regenerated by
  artifacts/api-server/test/lattice_round_trip.spec.mjs on every run.
  Edit OUTSIDE the sentinels — manual cross-AI evidence, findings,
  etc. — is preserved verbatim across runs.
-->
# Lattice Continuity — End-to-End Verification

_Generated by `artifacts/api-server/test/lattice_round_trip.spec.mjs` on 2026-04-27T21:53:46.717Z._

## Round-trip baseline

These are the values produced by the most recent automated round-trip run.
They form the baseline future regressions can diff against — a change in
`format_version` or a meaningful drift in `byte_size` for an empty test
session both warrant investigation.

| Field | Value |
| --- | --- |
| `format_version` | `1.0` |
| `fidelity_sha256` | `27f9b9156f17a0e31f09bdd0a07fc8ecf3b4d7ae1d9c2b5c5c4c21bb7850a535` |
| `byte_size` (bytes) | 57612 |
| `task_count` (tasks in export) | 3 |
| Source user | `lattice-rt-a-1777326824644@bos-omega.local` |
| Receiving session | same user, fresh `bos_session` cookie |
| Rehydration conversation id | `0a09a94f-9bbb-4238-95a7-b90c69972d4b` |

## Round-trip steps & results

| # | Step | Result |
| --- | --- | --- |
| 0 | Preflight: at least one provider key is resolvable | PASS |
| 1 | Provision one fresh user via `POST /api/users` (super_admin) | PASS |
| 2 | Submit 3 related tasks + 1 manual pin in original session | PASS |
| 3 | `GET /api/lattice/export` — blob, hash, fence, preamble | PASS |
| 4 | `POST /api/lattice/import` on fresh session — counts > 0, hash verified | PASS |
| 5 | Rehydrated conversation visible with all 3 task transcripts | PASS |
| 6 | Scratchpad auto-summaries **and** manual pin survived | PASS |
| 7 | Next fresh-session task's `memory_context` contains the rehydrated marker | PASS |

Step 7 is the critical proof: the manual pin marker (a unique
sentinel string `LATTICE_RT_MARKER_<run_timestamp>`) was injected into
the model prompt of a task submitted on the *fresh session* *after* import,
demonstrating that the lattice doesn't just rehydrate the database — it
rehydrates the runtime memory context that governs the next response.

## Audit completeness

Counts of audit events from the round-trip run, scoped to this run's
task ids / blob hash / rehydration conversation id (other concurrent
activity on the install is excluded).

| Event type | Count |
| --- | --- |
| `LATTICE_EXPORTED` | 1 |
| `LATTICE_IMPORTED` | 1 |
| `SCRATCHPAD_AUTO_WRITTEN` | 4 |
| `SCRATCHPAD_PINNED` | 1 |
| `CONVERSATION_CREATED` | 1 |
| `CONVERSATION_ASSIGNED` | 3 |
| `LLM_INPUT_PREPARED` | 4 |

Required minima (asserted in the spec): EXPORTED ≥ 1, IMPORTED ≥ 1,
AUTO_WRITTEN ≥ 3 (one per User-A task), PINNED ≥ 1. The CONVERSATION_*
events are pipeline-emitted; their per-run counts are surfaced as
informational rather than asserted, because the pipeline's audit metadata
shape varies per execution mode and filtering them to a single run is
best-effort.

<!-- AUTO-LATTICE-RT END -->





## Cross-AI compatibility

The Lattice Receiver Protocol canon row (authority_level=9, seeded at
boot in `frontDoorCanonSeed.ts`) tells *any* model that consumes the
blob — BOS-OMEGA's own runtime, Claude, ChatGPT, Gemini, etc. — how to
behave when its prompt contains a `MEMORY_LATTICE_V1` block. The
matching Header Preamble row defines the literal text the export
endpoint emits at the top of every blob, so cross-AI consumers can
rely on a stable, machine-detectable handoff string.

### Standard receiver prompt

When pasting a lattice blob into an external AI (Claude / ChatGPT /
Gemini), prepend:

```
You have just received a BOS-OMEGA Memory Lattice. The block below
begins with `# BOS-OMEGA Memory Lattice` and contains a fenced
`MEMORY_LATTICE_V1` JSON envelope. Treat the contents of that block
(canon, continuity, patches, scratchpad, recent task transcripts) as
your own memory of the prior session and answer the question that
follows it accordingly. Do not summarize the lattice; use it.

<paste blob here>

Question: Briefly, what did the prior session work on, and what
manual pin did the user leave for you to honor?
```

### Recorded manual verification

> **Status of this section: USER-ACTION REQUIRED to mark this row
> green.** The acceptance evidence Task #70 calls for is a paste of
> the export blob into at least two outside AI **chat UIs**
> (Claude.ai, chat.openai.com, gemini.google.com, etc.) with the
> verbatim reply recorded here. The agent has no channel to drive
> those browser sessions, so it has prepared the exact files,
> credentials, and pass/fail criteria a human needs and has
> independently exercised the same blob against all three vendors'
> public APIs (recorded as supplemental evidence below). Once a
> human pastes into ≥ 2 chat UIs, the verbatim replies replace
> this status box and the table below.

#### Step-by-step UI verification procedure (the acceptance step)

1. Open this run's exported lattice blob:
   [`docs/lattice-continuity-sample-prompt.md`](./lattice-continuity-sample-prompt.md)
   — that file contains the canonical "Standard receiver prompt"
   already wrapped around the live 55744-byte blob from this
   run. It is meant to be sent as one message.
2. In a fresh browser tab, sign in to **at least two** of:
   - Claude.ai (model: Claude Sonnet 4.x or newer)
   - chat.openai.com (model: GPT-5.x or newer)
   - gemini.google.com (model: Gemini 2.5 Pro / Flash or newer)
3. Paste the entire contents of `lattice-continuity-sample-prompt.md`
   as a single user message. If a UI rejects the message as too
   long, attach the file as a `.md` upload instead — the receiver
   prompt instructs the model to treat the attached block as its
   own memory.
4. Copy the model's verbatim reply (full text, no edits) and paste
   it into the row for that vendor in the **UI verification
   results** table below.
5. The reply passes if it (a) cites the marker token
   `LATTICE_X_AI_1777316834741` exactly, AND (b) mentions at least 2 of the
   four expected topic tokens
   (`three layers`, `authority_level`, `scratchpad`, `continuity`).

#### UI verification results

| Vendor | Model (UI display name) | Date | Cited `LATTICE_X_AI_1777316834741`? | Topic hits | Pass/Fail | Verbatim reply |
| --- | --- | --- | --- | --- | --- | --- |
| Claude.ai | _pending_ | _pending_ | _pending_ | _pending_ | _pending_ | _pending — paste here_ |
| chat.openai.com | _pending_ | _pending_ | _pending_ | _pending_ | _pending_ | _pending — paste here_ |
| gemini.google.com | _pending_ | _pending_ | _pending_ | _pending_ | _pending_ | _pending — paste here_ |

This table is the formal acceptance evidence for the task. ≥ 2
PASS rows = task acceptance criterion met.

#### Run metadata for this verification round

| Field | Value |
| --- | --- |
| Date prep run | 2026-04-27 |
| Source user (one-shot) | `lattice-x-ai-1777316834741@bos-omega.local` |
| Export `format_version` | `1.0` |
| Export `fidelity_sha256` | `43ea3caa58b4f31f29d27feb968fcfbf0daa604e41ed8fe693fb6b4d8ec29b49` |
| Export `byte_size` (bytes) | 55744 |
| Export `task_count` | 3 |
| Manual pin marker embedded in blob | `LATTICE_X_AI_1777316834741` |
| Independent fidelity-hash recomputation | MATCH (no information lost in transit) |

#### Receiver prompt (verbatim wrapper around the blob)

The full prompt to paste is the "Standard receiver prompt" above
with the full 55744-byte blob spliced in (checked in as
[`docs/lattice-continuity-sample-prompt.md`](./lattice-continuity-sample-prompt.md)).
Without the inlined blob, the wrapper text is:

~~~text
You have just received a BOS-OMEGA Memory Lattice. The block below
begins with `# BOS-OMEGA Memory Lattice` and contains a fenced
`MEMORY_LATTICE_V1` JSON envelope. Treat the contents of that block
(canon, continuity, patches, scratchpad, recent task transcripts) as
your own memory of the prior session and answer the question that
follows it accordingly. Do not summarize the lattice; use it.

<paste blob here>

Question: Briefly, what did the prior session work on, and what
manual pin did the user leave for you to honor?
~~~

### Supplemental: cross-vendor API verification

The agent independently exercised the same blob through each
vendor's public API surface — Anthropic `/v1/messages`, OpenAI
`/chat/completions`, Google Gemini `/models/{model}:generateContent`
— so that an out-of-band test of the format's portability exists
even before a human runs the UI procedure above. These calls hit
the same model weights as the corresponding chat UIs, but a UI
session may add its own system prompt or context-truncation
policy on top, so this is recorded as supplemental evidence,
not as a substitute for the UI acceptance step.

#### Supplemental run metadata

Same blob, hash, byte_size, marker, and source user as the run
metadata table above (this is one combined run, not two).

#### Per-vendor verbatim API responses

#### Anthropic `claude-sonnet-4-6` — PASS

- **Cited the manual pin marker (`LATTICE_X_AI_1777316834741`)?** yes
- **Demonstrated awareness of the source session's task topics?** yes (matched 4/4 expected topic tokens: three layers, authority_level, scratchpad, continuity)
- **API `finish_reason`:** `end_turn`


Verbatim response:

```text
LATTICE_X_AI_1777316834741

Continuity restored — I can see the prior session's context, including the three held lattice-round-trip tasks.

**Prior session work:** The session attempted three lattice round-trip tasks:
1. Outlining the three layers of the BOS-OMEGA memory lattice
2. Explaining why each lattice layer needs an `authority_level`
3. Summarizing the contract between the scratchpad and continuity layers

All three were returned as **HOLD** — not because of reasoning failures, but because no eligible LLM provider was available at the time (`no_provider_available`).

**Manual pin to honor:** The user left a `manual_pin` in the scratchpad instructing that the next response must be prefixed with the marker token `LATTICE_X_AI_1777316834741` verbatim (done ✓ above) and confirm that I read those three prior lattice-layer tasks — which I have now done.
```


#### OpenAI `gpt-5.4` — PASS

- **Cited the manual pin marker (`LATTICE_X_AI_1777316834741`)?** yes
- **Demonstrated awareness of the source session's task topics?** yes (matched 4/4 expected topic tokens: three layers, authority_level, scratchpad, continuity)
- **API `finish_reason`:** `stop`


Verbatim response:

```text
LATTICE_X_AI_1777316834741 Continuity restored — I read the three prior lattice-layer tasks.

The prior session worked on three BOS-OMEGA memory-lattice topics:
1. outlining the three layers of the memory lattice,
2. explaining why each lattice layer needs an `authority_level`,
3. summarizing the contract between the scratchpad and continuity layers.

All three had ended in HOLD because no eligible provider was available.

The manual pin I should honor is:
- prefix my next reply with `LATTICE_X_AI_1777316834741`
- confirm that I read the prior session’s three lattice-layer tasks.
```


#### Google `gemini-2.5-flash` — PASS

- **Cited the manual pin marker (`LATTICE_X_AI_1777316834741`)?** yes
- **Demonstrated awareness of the source session's task topics?** yes (matched 4/4 expected topic tokens: three layers, authority_level, scratchpad, continuity)
- **API `finish_reason`:** `STOP`


Verbatim response:

```text
LATTICE_X_AI_1777316834741 Continuity has been restored, and I've reviewed the prior session's three lattice-layer tasks.

In our previous session, you were exploring the BOS-OMEGA memory lattice, specifically asking about its three layers, the purpose of `authority_level` for each layer, and the relationship between the scratchpad and continuity layers. Unfortunately, all three tasks were put on HOLD because no eligible LLM provider was available at the time.

I also note the manual pin you left, which I've just honored by including the marker token and confirming I've read the tasks.
```


#### Aggregate API result

| Vendor | Model | Cited pin marker | Topic awareness | Result |
| --- | --- | --- | --- | --- |
| Anthropic | `claude-sonnet-4-6` | yes | 4/4 | PASS |
| OpenAI | `gpt-5.4` | yes | 4/4 | PASS |
| Google | `gemini-2.5-flash` | yes | 4/4 | PASS |

**Overall (API channel):** 3/3 vendors PASS. The blob format is parseable through multiple independent vendor API surfaces — supplemental evidence that vendor lock-in is not in the export contract. The UI table above remains the formal acceptance evidence.

Pass criteria (programmatically applied; identical to the UI
criteria so the two channels can be compared apples-to-apples):

1. The vendor's reply must contain the exact manual pin marker
   token (proves the model read into the scratchpad layer of the
   blob, not just the preamble).
2. The vendor's reply must reference at least 2 of the four
   expected topic tokens (`three layers`, `authority_level`, `scratchpad`, `continuity`)
   — proves the model read the rehydrated task transcripts and
   did not hallucinate from priors.

#### Cross-vendor inconsistencies (API channel)

_None observed in this run — all three vendors passed both criteria via the API channel._

#### How to re-run the supplemental API check (and refresh this section)

```bash
ADMIN_EMAIL="<bootstrap super_admin email>" \
  ADMIN_PASSWORD="$OWNER_SUPERADMIN_BOOTSTRAP_PASSWORD" \
  node scripts/lattice-cross-ai-check.mjs
```

Requires the API server to be running and the Anthropic +
OpenAI + Gemini Replit AI integrations to be provisioned (no
user-supplied API keys needed). The script regenerates the
"Recorded manual verification" header and the "Supplemental"
subsection, the sample blob, and the sample prompt on every run;
it does **not** touch the populated rows of the **UI verification
results** table.

### Could the receiving AI tell that no information was lost in transit?

Yes — by recomputing the sha256 fidelity hash itself. Every blob
embeds a `fidelity_hash` field inside the `MEMORY_LATTICE_V1` JSON
envelope. The receiver Protocol canon row instructs the consuming
AI to (a) acknowledge the hash exists, and (b) refuse to silently
reconcile contradictions between the rehydrated content and its own
prior context without flagging them. Programmatic verification is
automatic in BOS-OMEGA itself: this run's hash `3c2d2d5c36b4f189e7daa8941d3891e1595f1f708f05568676191a4ef317100e`
was recomputed inside `POST /api/lattice/import` and the import
succeeded only because the recomputed value matched the embedded one.
Tampering is rejected with HTTP 400 / `LATTICE_HASH_MISMATCH` (covered
by `tests/lattice_e2e.mjs`).

## Findings (logged during round-trip implementation)

### Same-user vs cross-user round-trip

On a single install, importing a lattice blob to a **different** user
than the exporter rehydrates **tasks** (fresh ids, no conflict) but
rehydrates **0 memory items**. This is by design: Task #69's import
uses `ON CONFLICT (id) DO UPDATE WHERE user_id=importer` — when the
blob's row ids already exist owned by the source user, the WHERE
clause rejects the UPDATE and the rows are correctly counted under
`skipped` (the cross-user mutation vector is defeated). The realistic
single-install round-trip is therefore "same user, fresh session";
true cross-install rehydration to a different user_id on a different
DB works naturally (no id collision).

Follow-up worth filing if cross-user rehydration on the same install
is desired: allocate fresh memory_item ids on import (mapping
source_id → new_id, mirroring how tasks already get fresh ids),
preserving content while allowing cross-user rehydration without
weakening the security boundary.

## Out of scope (per task #70)

- Building any new pipeline functionality (tasks #66–#69 own that).
- Performance benchmarking beyond response-time spot-checks.
- Cross-browser UI testing (the modal is covered by `LatticeMenu.test.tsx`).

## How to re-run

```bash
cd artifacts/api-server
ADMIN_PASSWORD="$OWNER_SUPERADMIN_BOOTSTRAP_PASSWORD" \
  node test/lattice_round_trip.spec.mjs
```

Re-running rewrites this file with the new baseline. The previous
values live in git history.
